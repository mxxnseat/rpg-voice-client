export const PLAYER_SIZES = {
  height: 100,
  width: 100,
};

export const USER_COMPONENT_FACTORY_KEY = Symbol.for(
  '__USER_COMPONENT_FACTORY__'
);

export const MAP_COMPONENT_FACTORY_KEY = Symbol.for(
  '__MAP_COMPONENT_FACTORY__'
);

export const OBJECT_COMPONENT_FACTORY_KEY = Symbol.for(
  '__OBJECT_COMPONENT_FACTORY__'
);
